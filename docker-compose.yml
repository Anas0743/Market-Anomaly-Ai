services:
  market-anomaly-ai:
    build: .
    image: market-anomaly-ai:latest
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      MARKET_DATA_FILE: "/data/market-history.example.json"
    volumes:
      - ./data:/data:ro
    restart: unless-stopped
